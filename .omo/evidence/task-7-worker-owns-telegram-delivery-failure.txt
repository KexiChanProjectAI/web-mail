=== task 7 failure-case evidence ===

The "failure" QA scenario for task 7 is: targeted search proves old Go
Telegram wording is removed and `/share/{token}` docs are retired. Below
are the queries and their results. Empty result = the wording IS removed
= the failure case is satisfied (the old behavior can no longer happen).

=== negative search: no Go TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID in production code/docs (outside .omo/ and .git/) ===

Command:
    grep -RnE "TELEGRAM_BOT_TOKEN|TELEGRAM_CHAT_ID" \
        --include="*.go" --include="*.md" --include="*.example" --include="*.toml" . \
        | grep -vE "^\./\.omo/" | grep -vE "^\./\.git/"

Result: empty (exit 1, no matches). PASS.

=== negative search: no cfg.Telegram* field/method references in any Go file ===

Command:
    grep -RnE "cfg\.TelegramBotToken|cfg\.TelegramChatID|cfg\.TelegramEnabled" --include="*.go" .

Result: empty (exit 1, no matches). PASS.

=== negative search: no TelegramEnabled method definition anywhere ===

Command:
    grep -RnE "func .* TelegramEnabled\(" --include="*.go" .

Result: empty. The method and its tests are gone. PASS.

=== negative search: no `Telegram Bot (optional...)` comment in .env.example ===

Command:
    grep -nE "Telegram Bot" .env.example

Result: empty. The two config lines and their explanatory comment are gone
from .env.example. PASS.

=== negative search: no "Telegram Email Forwarding" section in top-level README.md ===

Command:
    grep -nE "^## Telegram Email Forwarding|Telegram Email Forwarding" README.md

Result: empty. The old section is fully removed. PASS.

=== positive search: README.md now contains the Worker-owned Telegram forwarding paragraph ===

Command:
    grep -nE "Worker-owned Telegram forwarding|/share/\{token\}" README.md

Result:
    38:## Worker-owned Telegram forwarding
    50:**Old Go `/share/{token}` links are intentionally retired.** ...

PASS — the replacement content is in place and explicitly mentions
`/share/{token}` retirement.

=== positive search: worker/README.md documents the preview route, TTL warning, and env vars ===

Command:
    grep -nE "TTL-bound, not permanent|GET /email/:id\?mode|/email/:id\?mode=text|TELEGRAM_TOKEN|MAIL_TTL|MAX_EMAIL_SIZE|MAX_EMAIL_SIZE_POLICY|kv_namespaces|wrangler kv namespace create DB" worker/README.md

Result: 7+ matches, including the explicit `TTL-bound, not permanent` line,
the route description, every required env var, and the KV setup command.
PASS.

=== positive search: .env.example has 9 lines, no TELEGRAM_* keys ===

Command:
    wc -l .env.example
    grep -nE "TELEGRAM_" .env.example

wc -l: 9
TELEGRAM_: 0 matches. PASS.

=== positive search: wrangler.toml documents DB binding + secrets + TTL warning ===

Command:
    grep -nE "binding = .DB.|wrangler kv namespace create DB|TELEGRAM_TOKEN|TELEGRAM_ID|MAIL_TTL|MAX_EMAIL_SIZE" worker/wrangler.toml

Result: 6+ matches, including the `[[kv_namespaces]]` example block
showing `binding = "DB"`, the `wrangler kv namespace create DB` provisioning
command, every env var name, and the secret-vs-plaintext split. PASS.

=== positive search: .dev.vars.example documents all env vars with clear comments ===

Command:
    grep -nE "^TELEGRAM_TOKEN|^TELEGRAM_ID|^DOMAIN|^MAIL_TTL|^MAX_EMAIL_SIZE|^MAX_EMAIL_SIZE_POLICY|^INGEST_URL|^WORKER_INGEST_PSK" worker/.dev.vars.example

Result: all 8 env vars present, all commented with their purpose. PASS.

=== sanity: Go side is clean ===

`go vet ./...` and `go build ./...` both exit 0.
`go test -count=1 ./...` passes (10 packages, 0 failures, including the
`internal/config` package whose `TelegramEnabledWithCompleteEnv` and
`TelegramDisabledByDefault` tests were removed).

=== sanity: Worker side is clean ===

`cd worker && npm test` reports 60/60 pass across 5 test files.
`cd worker && npm run build` reports wrangler deploy --dry-run success.

=== summary ===

Every negative search returns no results (proving the old wording is
gone). Every positive search returns the expected matches (proving the
new Worker-owned documentation is in place). Task 7 acceptance criteria
are satisfied.

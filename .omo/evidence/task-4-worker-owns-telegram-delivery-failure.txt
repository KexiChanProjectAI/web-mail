Task 4 — Worker email-flow integration (failure / edge cases)
============================================================
Date: 2026-07-04
Plan: .omo/plans/worker-owns-telegram-delivery.md (todo 4)

This evidence file covers the "failure" scenarios required by the plan:
  (a) duplicate ingest must not write cache or send Telegram
  (b) ingest 401 / 413 / 5xx must not write cache, must not send Telegram,
      and must call setReject
  (c) parse failure must still POST raw MIME and (if Go accepts) must
      still write cache + send Telegram
  (d) Telegram send failure after accepted ingest must NOT call setReject
      (the email is already durably accepted)
  (e) opt-in: missing TELEGRAM_TOKEN or TELEGRAM_ID skips Telegram entirely

============================================================
1. TEST RUN — all failure / edge cases pass
============================================================
Command:
  $ cd worker && npx vitest run test/integration.test.ts -t "duplicate|ingest failure|parse failure|Telegram send failure|opt-out" --reporter=verbose

Output:
  ✓ test/integration.test.ts > email() flow — task 4 integration > duplicate ingest > (2) no KV write, no Telegram request, no setReject
  ✓ test/integration.test.ts > email() flow — task 4 integration > ingest failure > (3a) 401 → no KV, no Telegram, setReject called
  ✓ test/integration.test.ts > email() flow — task 4 integration > ingest failure > (3b) 413 → no KV, no Telegram, setReject called
  ✓ test/integration.test.ts > email() flow — task 4 integration > ingest failure > (3c) 5xx (after retry) → no KV, no Telegram, setReject called
  ✓ test/integration.test.ts > email() flow — task 4 integration > parse failure > (4) still POSTs raw MIME; if Go accepts, KV write + Telegram request still happen
  ✓ test/integration.test.ts > email() flow — task 4 integration > Telegram send failure after accepted ingest > (5) no setReject is called; KV write still happens
  ✓ test/integration.test.ts > email() flow — task 4 integration > opt-out: missing Telegram env > skips Telegram entirely if TELEGRAM_TOKEN is empty
  ✓ test/integration.test.ts > email() flow — task 4 integration > opt-out: missing Telegram env > skips Telegram entirely if TELEGRAM_ID is empty

  Test Files  1 passed (1)
       Tests  8 passed | 2 skipped (10)

============================================================
2. BEHAVIORAL INVARIANTS VERIFIED
============================================================

(a) Duplicate ingest
    - findGoCall()  : DEFINED (one POST to Go with raw MIME)
    - findTelegramCalls() : LENGTH 0 (no Telegram)
    - kv.__puts     : LENGTH 0 (no cache write)
    - message.setReject : NOT called

(b) Ingest 401
    - findTelegramCalls() : LENGTH 0
    - kv.__puts     : LENGTH 0
    - message.setReject called with "ingest authentication rejected"

    Ingest 413
    - findTelegramCalls() : LENGTH 0
    - kv.__puts     : LENGTH 0
    - message.setReject called with "message exceeds ingest size limits"

    Ingest 5xx (after retry)
    - mockFetch called TWICE (initial + retry)
    - findTelegramCalls() : LENGTH 0
    - kv.__puts     : LENGTH 0
    - message.setReject called with "ingest server returned status"

(c) Parse failure (broken MIME bytes)
    - findGoCall()  : DEFINED
    - Go POST body  : exact broken MIME bytes (decoder-equality verified)
    - kv.__puts     : LENGTH 1 (cache still written — parse fallback text/html)
    - stored.text   : non-empty string
    - findTelegramCalls() : > 0
    - message.setReject : NOT called

(d) Telegram send failure (5xx response from api.telegram.org)
    - findGoCall()  : DEFINED, status: "accepted"
    - kv.__puts     : LENGTH 1 (cache was written)
    - findTelegramCalls() : ≥ 2 (attempted per chat_id)
    - message.setReject : NOT called  ←  critical invariant
    - console.error logged with the failure description

(e) Opt-out
    Missing TELEGRAM_TOKEN:
    - findGoCall()  : DEFINED
    - kv.__puts     : LENGTH 1
    - findTelegramCalls() : LENGTH 0
    - message.setReject : NOT called

    Missing TELEGRAM_ID:
    - findGoCall()  : DEFINED
    - kv.__puts     : LENGTH 1
    - findTelegramCalls() : LENGTH 0
    - message.setReject : NOT called

============================================================
3. EXISTING 16 INGEST TESTS STILL PASS (no behavior regression)
============================================================
Command:
  $ cd worker && npx vitest run test/index.test.ts

Output:
  ✓ test/index.test.ts (16 tests) 203ms

All 16 pre-existing assertions still pass:
  - 401 authentication failure  → setReject
  - 413 message too large       → setReject
  - network error               → setReject
  - timeout (AbortError)        → setReject
  - 5xx response                → setReject (after retry)
  - 5xx retry succeeds          → no setReject
  - 5xx retry fails twice       → setReject
  - 4xx (no retry)              → setReject
  - missing INGEST_URL          → setReject
  - missing WORKER_INGEST_PSK   → setReject
  - stream read error           → setReject
  - empty raw stream            → POST sent, no setReject
  - accepted status logs        → "Email ingested successfully"
  - duplicate status logs       → "Duplicate email ignored"
  - POST to correct URL         → headers + body verified
  - raw stream collected        → bytes round-trip verified

============================================================
4. lsp_diagnostics — clean (modulo pre-existing baseline)
============================================================
src/handler/mail/index.ts: No diagnostics found.
src/index.ts:             1 pre-existing error (TS6133 'ctx' unused at 150:1).
test/integration.test.ts: 10 pre-existing-class errors (TS2345 MockContext.props).

No new error class introduced. The MockContext errors in the new test file
match the pre-existing pattern in test/index.test.ts; both are addressed by
the same out-of-scope follow-up (Partial<ExecutionContext> cast in
createMockContext).

Task 2 — Worker preview cache + `/email/:id` fetch surface
=============================================================

## Task 2 scope verification (PASS)

$ cd /home/kexi/web-mail/worker && npx vitest run test/fetch.test.ts
 ✓ test/fetch.test.ts (10 tests) 58ms
 Test Files  1 passed (1)
      Tests  10 passed (10)

Coverage matrix (all green):
  ✓ (a) serves cached text preview as text/plain; charset=utf-8
  ✓ (b) serves cached HTML preview as text/html; charset=utf-8
  ✓ (c)  returns 404 when the cache id is missing (mode=text)
  ✓ (c2) returns 404 for missing HTML preview (mode=html)
  ✓ (d)  returns 404 for an unsupported mode
  ✓ (e)  routes the /email/:id?mode=... path; non-preview paths 404
  ✓ Dao > loadMailCache returns null when key absent
  ✓ Dao > loadMailCache parses a previously-stored EmailCache from JSON
  ✓ Dao > saveMailCache writes JSON with explicit TTL
  ✓ Dao > saveMailCache omits TTL option when caller doesn't pass one

## Task 1 surface still green (no regressions)

$ cd /home/kexi/web-mail/worker && npx vitest run test/contract.test.ts test/index.test.ts
 ✓ test/contract.test.ts (8 tests)
 ✓ test/index.test.ts (16 tests)
 Total: 24/24 pass

## Build (PASS)

$ cd /home/kexi/web-mail/worker && npm run build
> wrangler deploy --dry-run
⛅️ wrangler 4.93.0
Total Upload: 12.16 KiB / gzip: 3.95 KiB
No bindings found.
--dry-run: exiting now.

## Tsc: my files clean (PASS)

$ cd /home/kexi/web-mail/worker && npx tsc --noEmit
- src/db/index.ts:               0 errors (lsp_diagnostics clean)
- src/handler/fetch/index.ts:    0 errors (lsp_diagnostics clean)
- test/fetch.test.ts:            0 errors (lsp_diagnostics clean)
- src/index.ts:                  1 pre-existing error (ctx unused, NOT mine)
- 27 total tsc errors, all in:
    - src/mail/parse.ts (parallel task 3 work, html-to-text types)
    - test/telegram.test.ts (parallel task 3 test, awaits render.ts)
    - test/index.test.ts MockContext.props (pre-existing at HEAD)
    - src/index.ts ctx unused (pre-existing at HEAD)

## npm test full-suite note

Running `cd worker && npm test` in isolation produces 10 failures,
all in test/telegram.test.ts — a test file that was added in
parallel by task 3 work. Task 2 owns:
  - test/fetch.test.ts (10 pass)
  - test/contract.test.ts (8 pass)
  - test/index.test.ts (16 pass)
= 34/34 pass for task-2-owned files.

Task 3 (Worker Telegram parser/client/renderer) is the owner of
test/telegram.test.ts and src/mail/parse.ts / src/mail/render.ts /
TelegramBotAPI.sendMessage. The plan explicitly allows task 2 and
task 3 to parallelize, and the parallel branch has not landed its
implementation yet (its tests reference the planned render.ts and
sendMessage — both task 3 deliverables).

This is expected per the plan's "Can parallelize with: 3"
permission on task 2; no task-2-owned file is broken by the
parallel task 3 work in flight.

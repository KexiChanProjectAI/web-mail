Task 4 — Worker email-flow integration (happy path)
====================================================
Date: 2026-07-04
Plan: .omo/plans/worker-owns-telegram-delivery.md (todo 4)
Module under test: worker/src/index.ts (email), worker/src/handler/mail/index.ts (emailHandler)

Files changed:
  - worker/src/index.ts            (refactor: parse → ingest → cache → telegram)
  - worker/src/handler/mail/index.ts (thin re-export over email())
  - worker/test/integration.test.ts (NEW — 10 integration assertions)

Files NOT changed (out of scope):
  - Go ingest endpoint and all *.go files
  - worker/src/handler/fetch/index.ts
  - worker/src/db/index.ts
  - worker/src/mail/parse.ts, render.ts
  - worker/src/telegram/api.ts
  - worker/src/types/index.ts

============================================================
1. HAPPY PATH — accepted ingest: one Go POST, one KV write, one Telegram request per chat_id
============================================================
Command:
  $ cd worker && npx vitest run test/integration.test.ts -t "happy path" --reporter=verbose

Output:
  ✓ test/integration.test.ts > email() flow — task 4 integration > happy path: accepted ingest > (1) one Go POST, one KV write, one Telegram request per chat_id
  ✓ test/integration.test.ts > email() flow — task 4 integration > happy path: accepted ingest > TELEGRAM_ID with a single chat id sends one Telegram request

  Test Files  1 passed (1)
       Tests  2 passed | 8 skipped (10)

Assertions covered:
  (a) exactly one Go POST to INGEST_URL with:
      - method=POST
      - Content-Type: message/rfc822
      - X-Lite-Mail-Ingest-PSK: <psk>
      - body = raw MIME bytes
  (b) exactly one KV put to env.DB:
      - key = cache.id
      - options = { expirationTtl: 86400 } (MAIL_TTL=86400 from env)
      - value = JSON.stringify(cache) with id, from, to, subject, text, html
  (c) exactly N Telegram POSTs (one per chat_id in TELEGRAM_ID="111,222"):
      - URL: https://api.telegram.org/bot<token>/sendMessage
      - chat_id values: ["111", "222"] (parsed by comma, trimmed)
  (d) message.setReject NOT called

============================================================
2. FULL SUITE — 60/60 pass
============================================================
Command:
  $ cd worker && npm test

Output:
  ✓ test/fetch.test.ts (10 tests) 84ms
  ✓ test/contract.test.ts (8 tests) 151ms
  ✓ test/telegram.test.ts (16 tests) 165ms
  ✓ test/integration.test.ts (10 tests) 189ms
  ✓ test/index.test.ts (16 tests) 203ms

  Test Files  5 passed (5)
       Tests  60 passed (60)

Breakdown:
  - 16 existing ingest tests (401, 413, network, timeout, 5xx, missing env, stream error)
  -  8 contract tests (Env shape, module surface, default + named exports)
  - 10 fetch tests (DAO save/load, /email/:id?mode=text|html)
  - 16 telegram tests (parse, render, API client, callback_data absence)
  - 10 NEW integration tests (this task)

============================================================
3. BUILD — succeeds
============================================================
Command:
  $ cd worker && npm run build

Output:
  > lite-mail-worker@0.1.0 build
  > wrangler deploy --dry-run

   ⛅️ wrangler 4.93.0 (update available 4.107.0)
  ───────────────────────────────────────────────
  Total Upload: 428.60 KiB / gzip: 121.40 KiB
  No bindings found.
  --dry-run: exiting now.

============================================================
4. tsc --noEmit — 27 errors, all pre-existing classes
============================================================
Command:
  $ cd worker && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l

Output: 27

Breakdown:
  -  1 × src/index.ts(150,2): TS6133 'ctx' is declared but its value is never read.
                                (PRE-EXISTING, mentioned in plan ISSUE-2)
  - 16 × test/index.test.ts(...): TS2345 MockContext missing props field
                                (PRE-EXISTING, mentioned in plan ISSUE-2)
  - 10 × test/integration.test.ts(...): TS2345 MockContext missing props field
                                (NEW but SAME class as pre-existing 16; same
                                 fix path — Partial<ExecutionContext> cast in
                                 createMockContext, out of scope per plan)

No new error class introduced. The `postToIngest` dead-code warning
that initially appeared was eliminated by removing the now-unused
function (its work is done by `postToIngestWithStatus` which the
new `email()` flow calls directly).

============================================================
5. INTEGRATION TEST COVERAGE MATRIX
============================================================
Scenario                                            | Test                          | Status
----------------------------------------------------|-------------------------------|-------
accepted ingest → 1 Go POST + 1 KV + 1 TG per id    | (1) one Go POST...            | PASS
accepted ingest with single chat_id                 | TELEGRAM_ID with a single...  | PASS
duplicate → 0 KV + 0 TG + no setReject              | (2) no KV write...            | PASS
401 ingest → 0 KV + 0 TG + setReject                | (3a) 401 → no KV...           | PASS
413 ingest → 0 KV + 0 TG + setReject                | (3b) 413 → no KV...           | PASS
5xx ingest (after retry) → 0 KV + 0 TG + setReject  | (3c) 5xx (after retry)...     | PASS
parse fail + accepted → 1 Go POST + 1 KV + 1 TG      | (4) still POSTs raw MIME...   | PASS
TG fail + accepted → 1 KV + 0 setReject              | (5) no setReject is called... | PASS
missing TELEGRAM_TOKEN → 1 Go + 1 KV + 0 TG          | skips Telegram...TOKEN empty  | PASS
missing TELEGRAM_ID    → 1 Go + 1 KV + 0 TG          | skips Telegram...ID empty     | PASS

Total: 10/10 integration tests pass.

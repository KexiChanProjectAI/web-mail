{
  "name": "lite-mail-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker for lite-mail ingest and worker-owned Telegram delivery (upstream-style module surface)",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.3.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.1.0"
  },
  "private": true,
  "dependencies": {
    "html-to-text": "^10.0.0",
    "itty-router": "^5.0.24",
    "postal-mime": "^2.7.5"
  }
}
